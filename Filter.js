function SushiOnOff() { 
    for(let i = 0; i < Sushi.length; i++) {
        Sushi[i].setMap(map);
    }
    
    for(let i = 0; i < Rice.length; i++) {
        Rice[i].setMap(null);
    }
    for(let i = 0; i < Noodle.length; i++) {
        Noodle[i].setMap(null);
    }
    
    for(let i = 0; i < Meat.length; i++) {
        Meat[i].setMap(null);
    }
    
    for(let i = 0; i < Coffee.length; i++) {
        Coffee[i].setMap(null);
    }
    
    for(let i = 0; i < Beer.length; i++) {
        Beer[i].setMap(null);
    }
    
    for(let i = 0; i < Bunsik.length; i++) {
        Bunsik[i].setMap(null);
    }
    
    for(let i = 0; i < Fastfood.length; i++) {
        Fastfood[i].setMap(null);
    }

    for(let i = 0; i < Building.length; i++) {
        Building[i].setMap(null);
    }
}

function MeatOnOff() { 
    for(let i = 0; i < Sushi.length; i++) {
        Sushi[i].setMap(null);
    }
    
    for(let i = 0; i < Rice.length; i++) {
        Rice[i].setMap(null);
    }
    for(let i = 0; i < Noodle.length; i++) {
        Noodle[i].setMap(null);
    }
    
    for(let i = 0; i < Meat.length; i++) {
        Meat[i].setMap(map);
    }
    
    for(let i = 0; i < Coffee.length; i++) {
        Coffee[i].setMap(null);
    }
    
    for(let i = 0; i < Beer.length; i++) {
        Beer[i].setMap(null);
    }
    
    for(let i = 0; i < Bunsik.length; i++) {
        Bunsik[i].setMap(null);
    }
    
    for(let i = 0; i < Fastfood.length; i++) {
        Fastfood[i].setMap(null);
    }

    for(let i = 0; i < Building.length; i++) {
        Building[i].setMap(null);
    }
}

function NoodleOnOff() { 
    for(let i = 0; i < Sushi.length; i++) {
        Sushi[i].setMap(null);
    }
    
    for(let i = 0; i < Rice.length; i++) {
        Rice[i].setMap(null);
    }
    for(let i = 0; i < Noodle.length; i++) {
        Noodle[i].setMap(map);
    }
    
    for(let i = 0; i < Meat.length; i++) {
        Meat[i].setMap(null);
    }
    
    for(let i = 0; i < Coffee.length; i++) {
        Coffee[i].setMap(null);
    }
    
    for(let i = 0; i < Beer.length; i++) {
        Beer[i].setMap(null);
    }
    
    for(let i = 0; i < Bunsik.length; i++) {
        Bunsik[i].setMap(null);
    }
    
    for(let i = 0; i < Fastfood.length; i++) {
        Fastfood[i].setMap(null);
    }

    for(let i = 0; i < Building.length; i++) {
        Building[i].setMap(null);
    }
}

function RiceOnOff() { 
    for(let i = 0; i < Sushi.length; i++) {
        Sushi[i].setMap(null);
    }
    
    for(let i = 0; i < Rice.length; i++) {
        Rice[i].setMap(map);
    }
    for(let i = 0; i < Noodle.length; i++) {
        Noodle[i].setMap(null);
    }
    
    for(let i = 0; i < Meat.length; i++) {
        Meat[i].setMap(null);
    }
    
    for(let i = 0; i < Coffee.length; i++) {
        Coffee[i].setMap(null);
    }
    
    for(let i = 0; i < Beer.length; i++) {
        Beer[i].setMap(null);
    }
    
    for(let i = 0; i < Bunsik.length; i++) {
        Bunsik[i].setMap(null);
    }
    
    for(let i = 0; i < Fastfood.length; i++) {
        Fastfood[i].setMap(null);
    }

    for(let i = 0; i < Building.length; i++) {
        Building[i].setMap(null);
    }
}

function BunsikOnOff() { 
    for(let i = 0; i < Sushi.length; i++) {
        Sushi[i].setMap(null);
    }
    
    for(let i = 0; i < Rice.length; i++) {
        Rice[i].setMap(null);
    }
    for(let i = 0; i < Noodle.length; i++) {
        Noodle[i].setMap(null);
    }
    
    for(let i = 0; i < Meat.length; i++) {
        Meat[i].setMap(null);
    }
    
    for(let i = 0; i < Coffee.length; i++) {
        Coffee[i].setMap(null);
    }
    
    for(let i = 0; i < Beer.length; i++) {
        Beer[i].setMap(null);
    }
    
    for(let i = 0; i < Bunsik.length; i++) {
        Bunsik[i].setMap(map);
    }
    
    for(let i = 0; i < Fastfood.length; i++) {
        Fastfood[i].setMap(null);
    }

    for(let i = 0; i < Building.length; i++) {
        Building[i].setMap(null);
    }
}

function FastfoodOnOff() { 
    for(let i = 0; i < Sushi.length; i++) {
        Sushi[i].setMap(null);
    }
    
    for(let i = 0; i < Rice.length; i++) {
        Rice[i].setMap(null);
    }
    for(let i = 0; i < Noodle.length; i++) {
        Noodle[i].setMap(null);
    }
    
    for(let i = 0; i < Meat.length; i++) {
        Meat[i].setMap(null);
    }
    
    for(let i = 0; i < Coffee.length; i++) {
        Coffee[i].setMap(null);
    }
    
    for(let i = 0; i < Beer.length; i++) {
        Beer[i].setMap(null);
    }
    
    for(let i = 0; i < Bunsik.length; i++) {
        Bunsik[i].setMap(null);
    }
    
    for(let i = 0; i < Fastfood.length; i++) {
        Fastfood[i].setMap(map);
    }

    for(let i = 0; i < Building.length; i++) {
        Building[i].setMap(null);
    }
}

function CoffeeOnOff() { 
    for(let i = 0; i < Sushi.length; i++) {
        Sushi[i].setMap(null);
    }
    
    for(let i = 0; i < Rice.length; i++) {
        Rice[i].setMap(null);
    }
    for(let i = 0; i < Noodle.length; i++) {
        Noodle[i].setMap(null);
    }
    
    for(let i = 0; i < Meat.length; i++) {
        Meat[i].setMap(null);
    }
    
    for(let i = 0; i < Coffee.length; i++) {
        Coffee[i].setMap(map);
    }
    
    for(let i = 0; i < Beer.length; i++) {
        Beer[i].setMap(null);
    }
    
    for(let i = 0; i < Bunsik.length; i++) {
        Bunsik[i].setMap(null);
    }
    
    for(let i = 0; i < Fastfood.length; i++) {
        Fastfood[i].setMap(null);
    }

    for(let i = 0; i < Building.length; i++) {
        Building[i].setMap(null);
    }
}

function BeerOnOff() { 
    for(let i = 0; i < Sushi.length; i++) {
        Sushi[i].setMap(null);
    }
    
    for(let i = 0; i < Rice.length; i++) {
        Rice[i].setMap(null);
    }
    for(let i = 0; i < Noodle.length; i++) {
        Noodle[i].setMap(null);
    }
    
    for(let i = 0; i < Meat.length; i++) {
        Meat[i].setMap(null);
    }
    
    for(let i = 0; i < Coffee.length; i++) {
        Coffee[i].setMap(null);
    }
    
    for(let i = 0; i < Beer.length; i++) {
        Beer[i].setMap(map);
    }
    
    for(let i = 0; i < Bunsik.length; i++) {
        Bunsik[i].setMap(null);
    }
    
    for(let i = 0; i < Fastfood.length; i++) {
        Fastfood[i].setMap(null);
    }

    for(let i = 0; i < Building.length; i++) {
        Building[i].setMap(null);
    }
}

function BuildingOnOff() { 
    for(let i = 0; i < Sushi.length; i++) {
        Sushi[i].setMap(null);
    }
    
    for(let i = 0; i < Rice.length; i++) {
        Rice[i].setMap(null);
    }
    for(let i = 0; i < Noodle.length; i++) {
        Noodle[i].setMap(null);
    }
    
    for(let i = 0; i < Meat.length; i++) {
        Meat[i].setMap(null);
    }
    
    for(let i = 0; i < Coffee.length; i++) {
        Coffee[i].setMap(null);
    }
    
    for(let i = 0; i < Beer.length; i++) {
        Beer[i].setMap(null);
    }
    
    for(let i = 0; i < Bunsik.length; i++) {
        Bunsik[i].setMap(null);
    }
    
    for(let i = 0; i < Fastfood.length; i++) {
        Fastfood[i].setMap(null);
    }

    for(let i = 0; i < Building.length; i++) {
        Building[i].setMap(map);
    }
}

function FoodOnOff() { 
    for(let i = 0; i < Sushi.length; i++) {
        Sushi[i].setMap(map);
    }
    
    for(let i = 0; i < Rice.length; i++) {
        Rice[i].setMap(map);
    }
    for(let i = 0; i < Noodle.length; i++) {
        Noodle[i].setMap(map);
    }
    
    for(let i = 0; i < Meat.length; i++) {
        Meat[i].setMap(map);
    }
    
    for(let i = 0; i < Coffee.length; i++) {
        Coffee[i].setMap(null);
    }
    
    for(let i = 0; i < Beer.length; i++) {
        Beer[i].setMap(null);
    }
    
    for(let i = 0; i < Bunsik.length; i++) {
        Bunsik[i].setMap(map);
    }
    
    for(let i = 0; i < Fastfood.length; i++) {
        Fastfood[i].setMap(map);
    }

    for(let i = 0; i < Building.length; i++) {
        Building[i].setMap(null);
    }
}

